#!/bin/bash
# GNOME Console entry point: ensure the arch distrobox exists (image pulled on
# first use), then drop the user into an interactive shell inside it.
[ -f /etc/archlinux ] && bash /etc/archlinux
exec distrobox enter archlinux
